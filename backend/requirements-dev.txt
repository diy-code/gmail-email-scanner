# Development / testing dependencies
# Install with: pip install -r requirements-dev.txt
# (requirements.txt must be installed first)

pytest>=8.3
pytest-asyncio>=0.24        # async test support; asyncio_mode=auto via pytest.ini
httpx>=0.27                  # AsyncClient for FastAPI integration tests (matches prod pin)
respx>=0.21                  # HTTPX request mocking for signal-engine unit tests
